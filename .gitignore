# Segredos nunca entram aqui - referenciar via EnvironmentFile= apontando
# pra fora do repo, nunca commitar o arquivo de valores em si.
*.secret.env
*-secrets.*

# Estado local do Claude Code (git worktrees de sessões, settings locais)
.claude/
