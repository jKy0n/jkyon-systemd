# Idempotente com shared/root/sccache-dist-server — mesmo diretório, garante
# que o scheduler funcione sozinho aqui mesmo se algum dia o pacote server
# não estiver stowado no Builder.
d /etc/sccache-dist 0700 root root -
